{{#with data}}
<div class="post">

    <strong class="post_title">{{title}}</strong>

    <div class="post_info">{{date}}<span class="post_author">{{autor}}</span>

        <p class="post_description">{{description}}</p>

        <a href="#">Continue reading</a>
    </div>

</div>
{{/with}}